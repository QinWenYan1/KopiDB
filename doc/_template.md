# NN-模块名（如 01-skiplist）

> RocksDB 版本：`e6a2ee0` · 对应 KopiDB 模块：`src/xxx` · 状态：草稿 / 已精读

## 1. 模块职责

一句话说明这个模块是什么、在整体架构中的位置。

## 2. 核心 workflow / 调用链

以一次 Put/Get 的完整旅程为主线，经过本模块时发生什么。关键调用给 `文件:行号`。

## 3. 关键数据结构

核心类 / 结构体，字段含义，为什么这样组织。

## 4. 设计取舍（为什么）

**面试重点。** 每个决策按三段式写：做了什么选择 → 为什么 → 代价是什么。

## 5. KopiDB vs RocksDB 对照

【自己填写】KopiDB 怎么做的 / RocksDB 怎么做的 / RocksDB 为什么更复杂（它解决了什么教学版没有的问题）

## 6. 自测题

【合上笔记，用自己的话作答】

1. ……

## 7. 待验证点

【到源码核实】格式：结论 → `文件:行号` → ✅/❌ → 若有误，记录正确结论

1. ……
